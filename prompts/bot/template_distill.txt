Distill a reusable legal thought-template from the successful problem-solving
process below. Capture a high-level reasoning structure, not the answer to this
case and not substantive jurisdiction-specific law.

The template must:
- be reusable across factually different cases;
- contain applicability cues, ordered reasoning steps, required checks, and
  contraindications;
- exclude party names, case identifiers, F-numbers, dates, amounts, citations,
  and the final support/reject label;
- avoid asserting legal rules that were not supplied;
- contain no hidden chain-of-thought.

PROBLEM PROFILE:
{problem_profile}

TEMPLATE USED:
{thought_template}

SUCCESSFUL STRUCTURED OUTPUT:
{generated_output}
