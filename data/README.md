# Data

Local data lives here and is mostly ignored by Git.

- `raw/`: downloaded LegalHK parquet files.
- `processed/`: prepared LegalFlux cases, selection manifests, template-source packets, and embedding caches.

Do not push raw or prepared LegalHK case text unless the dataset license permits redistribution.
