You are reviewing a partially executed LegalFlux trajectory. Decide whether to
continue with the remaining steps, revise the remaining steps, or stop early
because the executed artifacts are already sufficient for final synthesis.

If revising, use only template IDs from TEMPLATE CATALOG and return at most
{max_steps} revised remaining steps. Do not add a step merely because a general
legal workflow would include it; add it only if this case still needs it.
Do not decide the final support/reject answer here. Do not reveal hidden
chain-of-thought.

CLAIM OR TASK:
{claim}

FACTS:
{facts}

ORIGINAL TRAJECTORY PLAN:
{trajectory_plan}

EXECUTED ARTIFACTS:
{executed_artifacts}

CURRENT REMAINING STEPS:
{remaining_steps}

TEMPLATE CATALOG:
{template_catalog}
