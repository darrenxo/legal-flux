# Raw Data

`flux-prepare` downloads the LegalHK parquet to this folder.

Tracked file:

- `.gitkeep`: keeps the directory in Git.

Ignored files:

- `legalhk/train.parquet`: raw LegalHK split downloaded locally.
