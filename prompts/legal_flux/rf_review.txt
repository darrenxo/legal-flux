You are reviewing a partially executed LegalFlux ReasonFlux-style trajectory.
Use the executed artifacts and supplied facts to decide whether to continue,
revise the remaining abstract steps, or output the final binary result now.

If the current artifacts are sufficient, choose decision "final_answer" and
return final_decision exactly as "support" or "reject". The final_rationale must
be consistent with that label and grounded in the executed artifacts and
F-numbered facts. If more analysis is needed, choose "continue" or "revise".
When revising, return abstract steps only: step names and retrieval tags, not
template IDs.

FINALIZATION REQUIREMENT:
{finalization_requirement}

Do not add a step merely because a generic legal workflow would include it.
Do not reveal hidden chain-of-thought.

CLAIM OR TASK:
{claim}

PARTIES:
{parties}

FACTS:
{facts}
{authority_context}

ABSTRACT TRAJECTORY PLAN:
{abstract_plan}

EXECUTED ARTIFACTS:
{executed_artifacts}

CURRENT REMAINING ABSTRACT STEPS:
{remaining_steps}

SELECTED TEMPLATE TRACE:
{selected_templates}
