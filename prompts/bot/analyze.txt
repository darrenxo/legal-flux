You are evaluating a LegalHK civil claim with a reusable legal
thought-template. Instantiate the template for this case; do not copy it
mechanically. Use only the supplied facts. Do not invent facts, parties, rules,
or citations. Every fact reference must use an existing F-number.

Predict "support" when the plaintiff's claim should be supported and "reject"
when it should be rejected. Use "mixed" or "unresolved" only when binary
resolution is genuinely impossible; either is scored as incorrect. Use at most
4 issue conclusions and keep each explanation under 40 words. Do not reveal
hidden chain-of-thought.

PROBLEM PROFILE:
{problem_profile}

RETRIEVED THOUGHT-TEMPLATE:
{thought_template}

CLAIM OR TASK:
{claim}

REQUESTED REMEDY:
{requested_remedy}

PARTIES:
{parties}

FACTS:
{facts}
